# Development Dockerfile for React/Vite with hot reload
FROM node:22-alpine

WORKDIR /app

# Install dependencies first (for caching)
COPY package*.json ./
RUN npm ci

# Copy source (will be overridden by volume mounts)
COPY . .

EXPOSE 5173

# Default command - Vite dev server with HMR
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
